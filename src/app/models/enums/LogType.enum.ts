export enum LogType {
    // Ayarlarlar ilgili olanlar
    System,
    // auth bilgileri (login oldu, olamadı gibi)
    Security,
    // config in dışındaki crud (nap yarattı, sildi gibi)
    Information
}
